import pytest

def test_player_name():
    assert callable(functions.player_name())

def test_what_now():
    assert callable(functions.what_now())
        
def test_option_stairs():
    assert callable(functions.option_stairs())
    
def test_down_stairs():
    assert callable(functions.down_stairs())

